import { ComingSoon } from '@/components/ComingSoon';

export default function BudgetScreen() {
  return <ComingSoon canClose title="Anggaran" subtitle="Batas per kategori + peringatan over-budget — segera." />;
}
