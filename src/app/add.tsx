import { ComingSoon } from '@/components/ComingSoon';

export default function AddScreen() {
  return <ComingSoon canClose title="Tambah Transaksi" subtitle="Pemasukan / Pengeluaran / Transfer dengan alur khusus — segera." />;
}
