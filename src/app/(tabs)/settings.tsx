import { ComingSoon } from '@/components/ComingSoon';

export default function SettingsScreen() {
  return <ComingSoon title="Pengaturan" subtitle="Mata uang, notifikasi, ekspor, dan akun — segera." />;
}
